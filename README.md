# palantir-style-semantic-layer
This project is a reference implementation of a semantic control plane that enables LLM/Agents to safely execute enterprise queries without relying on human interpretation at runtime.
